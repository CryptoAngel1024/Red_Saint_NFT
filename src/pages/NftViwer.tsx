import React from 'react'
import styled from 'styled-components'

const NftViewerWrapper = styled.div`
  display: flex;
  flex-wrap: wrap;
`

const NftViwer = () => {
  return <NftViewerWrapper className="mt-medium section-wrapper ">{/* <ViewerCard  /> */}</NftViewerWrapper>
}

export default NftViwer
